#pragma once

#include "properties/registration_basics.hpp"

#include "properties/access/enum_access_registration.hpp"

namespace j::properties {
  using access::enum_access_registration;
  using strings::formatters::enum_value;
}
